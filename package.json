{
  "name": "agentos",
  "version": "0.1.0",
  "private": true,
  "description": "AgentOS — a visual, clickable agentic OS wrapping Claude Code",
  "workspaces": [
    "server",
    "web"
  ],
  "scripts": {
    "dev": "concurrently -n server,web -c cyan,magenta \"npm run dev -w server\" \"npm run dev -w web\"",
    "build": "npm run build -w server && npm run build -w web"
  },
  "devDependencies": {
    "concurrently": "latest"
  }
}
